## Switch

Go doesn't requre the break keyword at the end of a case to stop it from falling through to the next case.
the `break` statement is implicit in Go.

If you do want a `case` to fall through to the next case, you can use the `fallthough` keyword.
