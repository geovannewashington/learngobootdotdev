# Structs in Go

Structs are a way of defining our own custom data by grouping togueter other data.

We can access it's values with dot notation, similar to objects in JS.

```go
type car struct {
	brand      string
	model      string
	doors      int
	mileage    int
}
```
